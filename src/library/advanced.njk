---
layout: app
path: library/advanced
# Non public facing

title: Advanced // Delete this file from production sites
description:
---

<!-- prettier-ignore -->
{% set hero_simple = ({
  title: 'Advanced',
  desc: 'More complex components, advanced js, webGL, spline, svg shaders, etc.'
}) %}
{% include '../_includes/library/sections/hero-simple.njk' %}

<!-- Mouse position seems affected by content placement -->
<section class="dots-field">
  <h2 class="h5">[ <span class="glitch-scroll">dots</span> ]</h2>
  <!-- prettier-ignore -->
  {% set dots = ({
    width: '400',
    height: '400'
   }) %}
  {% include "../_includes/library/advanced/dots.njk" %}

  <!-- prettier-ignore -->
  {% set dots = ({
    width: '800',
    height: '280',
    size: 5,
    gap: 16,
    restore: 0.0001,
    sensitivity: 0.5,
    distance: 100,
    strength: 0,
    illuminate: true,
    grow: true
   }) %}
   {% include "../_includes/library/advanced/dots.njk" %}

  <!-- prettier-ignore -->
  {% set dots = ({
    width: '180',
    height: '240',
    size: 1,
    gap: 12,
    restore: 0.01,
    sensitivity: 0.9,
    distance: 150,
    strength: 25,
    grow: true
   }) %}
  {% include "../_includes/library/advanced/dots.njk" %}

  <!-- prettier-ignore -->
  {% set dots = ({
    width: '600',
    height: '420',
    size: 2,
    gap: 16,
    restore: 0,
    sensitivity: 0.8,
    distance: 200,
    strength: 500,
    illuminate: true,
    grow: true
   }) %}
   {% include "../_includes/library/advanced/dots.njk" %}
</section>

<section
  class="grid-show"
  data-grid-enter="ripple-out"
  data-grid-exit="ripple-out"
  data-stagger-interval="50"
>
  <h2 class="h4">Grid Background</h2>
  <p>
    A subtle and modern background effect. Just add
    <code>&lt;canvas id="grid-bg"&gt;&lt;/canvas&gt;</code> at the bottom of
    your app to get started.
  </p>
  <p>
    Out of the box this will follow the user's mouse cursor. It will also have
    an interval that shows the grid in an animation of your choice. This is so
    users without a mouse can still enjoy the effect. Current animation options
    include:
  </p>
  <ul class="classic-list">
    <li><code>stack-top</code></li>
    <li><code>stack-bottom</code></li>
    <li><code>stack-left</code></li>
    <li><code>stack-right</code></li>
    <li><code>ripple-out</code></li>
    <li><code>ripple-in</code></li>
    <li><code>random</code></li>
    <li><code>diag-tl</code></li>
    <li><code>diag-tr</code></li>
    <li><code>diag-br</code></li>
    <li><code>diag-bl</code></li>
  </ul>
  <p>
    You can also tack on the util class of <code>grid-show</code> to fade the
    grid in (as you're currently seeing).
  </p>
  <p>Fully configurable with:</p>
  <ul class="classic-list">
    <li><code>data-grid-enter</code> to handle the 'enter' animation.</li>
    <li><code>data-grid-exit</code> to handle the 'exit' animation.</li>
    <li>
      <code>data-stagger-interval</code> a number do alter the animation speed.
    </li>
  </ul>
</section>

<section class="grid-flow-img-section">
  <!-- prettier-ignore -->
  {% set grid_flow_img_src = '/static/img/library/landscape-1.webp' %}
  {% set grid_flow_img_alt = '' %}
  {% set grid_flow_img_size = 'lg' %}
  {% include '../_includes/library/advanced/grid-flow-img.njk' %}
  
  {% set grid_flow_img_src = '/static/img/library/landscape-2.webp' %}
  {% set grid_flow_img_alt = '' %}
  {% set grid_flow_img_size = 'md' %}
  {% include '../_includes/library/advanced/grid-flow-img.njk' %}
</section>

<section class="spline-section library-section">
  <h2 class="h6">[ Spline ]</h2>

  <canvas id="spline-canvas"></canvas>
  <!-- https://prod.spline.design/d7HscHvJYQfAtvcZ/scene.splinecode -->
</section>

<section class="video-scrub-section">
  <video
    class="video-scrub"
    src="/static/img/library/vid-sample.mp4"
    preload="auto"
    muted
    playsinline
  ></video>
</section>

<!-- prettier-ignore -->
{% include '../_includes/partials/loader.njk' %}
