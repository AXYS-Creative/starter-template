---
layout: app
path: library/advanced
# Non public facing

title: Advanced // Delete this file from production sites
description:
---

<!-- prettier-ignore -->
{% set hero_simple = ({
  title: 'Advanced',
  desc: 'More complex components, advanced js, webGL, spline, svg shaders, etc.'
}) %}
{% include '../_includes/library/sections/hero-simple.njk' %}

<section class="bg-particle-waves">
  <div class="bg-particle-waves__inner">
    <h2>[<span class="typing-scroll"> Particle waves </span>]</h2>
    <small>
      Use the <code>bg-particle-waves</code> class on the section, then include the canvas at the
      bottom. <br class="md-up" />
      Comes with a few handy configs.
    </small>
  </div>

  <!-- prettier-ignore -->
  {% set particle_waves = ({
    gap: 0.4,
    color: 'var(--color-brand--secondary)',
    opacity: 0.2,
    follow_mouse: 20,
    speed: 0.005
  }) %}
  {% include "../_includes/library/advanced/particle-waves.njk" %}
</section>

<!-- Mouse position seems affected by content placement -->
<section class="dots-field">
  <h2>[ <span class="glitch-scroll">dots</span> ]</h2>
  <!-- prettier-ignore -->
  {% set dots = ({
    width: '25%',
    height: '400'
   }) %}
  {% include "../_includes/library/advanced/dots.njk" %}

  <!-- prettier-ignore -->
  {% set dots = ({
    width: '50%',
    height: '280',
    size: 5,
    gap: 16,
    restore: 0.0001,
    sensitivity: 0.5,
    distance: 100,
    strength: 0,
    color: 'var(--color-brand--secondary)',
    illuminate: true,
    grow: true
   }) %}
   {% include "../_includes/library/advanced/dots.njk" %}

  <!-- prettier-ignore -->
  {% set dots = ({
    width: '180',
    height: '240',
    size: 1,
    gap: 12,
    restore: 0.01,
    sensitivity: 0.9,
    distance: 180,
    strength: 25,
    grow: true
   }) %}
  {% include "../_includes/library/advanced/dots.njk" %}

  <!-- prettier-ignore -->
  {% set dots = ({
    width: '40%',
    height: '420',
    size: 2,
    gap: 16,
    restore: 0,
    sensitivity: 0.8,
    distance: 200,
    strength: 500,
    color: 'var(--color-brand--primary)',
    illuminate: true,
    grow: true
   }) %}
   {% include "../_includes/library/advanced/dots.njk" %}
</section>

<section
  class="grid-show"
  data-grid-enter="ripple-out"
  data-grid-exit="ripple-out"
  data-stagger-interval="50"
>
  <h2>Grid Background</h2>
  <p>
    A subtle and modern background effect. Just add
    <code>&lt;canvas id="grid-bg"&gt;&lt;/canvas&gt;</code> at the bottom of your app to get
    started.
  </p>
  <p>
    Out of the box this will follow the user's mouse cursor. It will also have an interval that
    shows the grid in an animation of your choice. This is so users without a mouse can still enjoy
    the effect. Current animation options include:
  </p>
  <ul class="classic-list">
    <li><code>stack-top</code></li>
    <li><code>stack-bottom</code></li>
    <li><code>stack-left</code></li>
    <li><code>stack-right</code></li>
    <li><code>ripple-out</code></li>
    <li><code>ripple-in</code></li>
    <li><code>random</code></li>
    <li><code>diag-tl</code></li>
    <li><code>diag-tr</code></li>
    <li><code>diag-br</code></li>
    <li><code>diag-bl</code></li>
  </ul>
  <p>
    You can also tack on the util class of <code>grid-show</code> to fade the grid in (as you're
    currently seeing).
  </p>
  <p>Fully configurable with:</p>
  <ul class="classic-list">
    <li><code>data-grid-enter</code> to handle the 'enter' animation.</li>
    <li><code>data-grid-exit</code> to handle the 'exit' animation.</li>
    <li><code>data-stagger-interval</code> a number do alter the animation speed.</li>
  </ul>
</section>

<section class="grid-flow-img-section">
  <h2>[ <span class="typing-scroll">grid flow img</span> ]</h2>

  <!-- prettier-ignore -->
  {% set grid_flow_img = ({
    src: '/static/img/library/img-sample-hd.jpg',
    alt: ''
  }) %}
  {% include '../_includes/library/advanced/grid-flow-img.njk' %}

  <!-- prettier-ignore -->
  {% set grid_flow_img = ({
    src: '/static/img/library/img-sample-8.jpg',
    alt: 'Scenery of Reach',
    width: 350,
    height: 600,
    lazy_load: false,
    size: 'sm',
    orientation: 'portrait',
    fit: 'cover',
    grid: 12,
    ease: 0.04,
    range: 0.5,
    intensity: 1
  }) %}
  {% include '../_includes/library/advanced/grid-flow-img.njk' %}

  <!-- prettier-ignore -->
  {% set grid_flow_img = ({
    src: '/static/img/library/landscape-2.webp',
    alt: '',
    size: 'md',
    lazy_load: false,
    greyscale: false,
    grid: 20,
    ease: 0.064,
    range: 0.2,
    intensity: 1.5
  }) %}
  {% include '../_includes/library/advanced/grid-flow-img.njk' %}
</section>

<section class="spline-section library-section">
  <h2>[ Spline ]</h2>

  <canvas id="spline-canvas"></canvas>
  <!-- https://prod.spline.design/d7HscHvJYQfAtvcZ/scene.splinecode -->
</section>

<section class="video-scrub-section last-section">
  <video
    class="video-scrub"
    src="/static/img/library/vid-sample.mp4"
    preload="auto"
    muted
    playsinline
  ></video>
</section>

<!-- prettier-ignore -->
{% include '../_includes/partials/loader.njk' %}
